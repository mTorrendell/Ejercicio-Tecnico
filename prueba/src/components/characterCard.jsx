function CharacterCard({ character }) {

  return (
    <>
      <div>{character.id}</div>
      <div>{character.id}</div>
      <div>{character.id}</div>
      <div>{character.id}</div>
      <div>{character.id}</div>
      <div>{character.id}</div>
    </>
  );
}

export default CharacterCard;
